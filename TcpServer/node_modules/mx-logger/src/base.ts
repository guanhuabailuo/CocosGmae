export interface ifLoggerKeeper {
    name: string;
    init(): Promise<boolean>;
    logEvent(uid: string, event_name: string, obj: Object): any
    logStatic(uid: string, eventType: string, obj: Object): any
    logApi(api: string, method: string, uid: string, succ: boolean, consumeTime: number, reqID: string, param: string, response: string, isShowAll?: boolean): any
}